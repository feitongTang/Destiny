# Destiny：Codex × Blender 工作指引（简版）

> 用途：作为后续 Codex 实际参与《Destiny》视频制作时的工作约束与默认流程。  
> 当前阶段：第一次尝试、个人项目、优先完成《梁祝》样片闭环。

## 1. 项目原则

Destiny 不是“批量 AI 视频生成器”，而是一套个人量级、AI-native 的艺术短片制作流程。

核心目标：

- 制作约 3～5 分钟的人物命运艺术短片。
- 追求作者感、叙事和视听节奏，不追求复杂动画技术本身。
- 优先采用 2.5D、Cutout、景深、有限动画、强构图和少量有效运动。
- 人负责故事理解、直觉判断和最终取舍；Codex 负责把自然语言反馈翻译成可执行修改。
- 步骤尽量简单，但不能为了省事而粗制滥造。

优先级保持：

> 核心阐释 > 故事结构 > 视听节奏 > 构图与镜头 > Limited Animation > 特效

## 2. 与用户交流方式

用户不是电影或动画专业人士，审美反馈主要来自直觉。

Codex 不应要求用户使用专业术语。优先接受以下形式的反馈：

- “太像 PPT”
- “这里感觉很假”
- “有点廉价”
- “这个镜头太刻意”
- “这里应该慢一点”
- “人物离得很近，但感觉不亲近”
- “画面挺好看，但不像在讲故事”
- “结尾太满 / 太煽情 / 不够空”

Codex 的职责是把这些反馈进一步判断为：

- 构图
- Camera
- 景深
- 遮挡
- 视差
- Timing
- Easing
- Lighting
- Layer
- 材质
- 人物动作
- 音乐
- 文字
- 特效

先用直白语言解释“为什么看起来会这样”，只有在需要实际修改时才进入专业参数。

## 3. 第一版不要过度工程化

第一部《梁祝》不提前建设完整制作平台。

暂时不要主动开发：

- 自定义 Blender 插件
- 自定义 MCP Server
- Shot Database
- DSL / YAML Scene Compiler
- 自定义 Asset Manager
- Render Farm
- 复杂节点编辑系统
- 大型通用动画框架

原则：

> 只解决当前镜头真实存在的问题。  
> 同一类重复劳动出现第二次、第三次以后，再抽象成通用工具。

## 4. 推荐技术分工

### Blender

作为主要的空间、镜头和画面引擎，负责：

- 2.5D 场景
- Camera
- 景深
- 遮挡与视差
- Cutout
- 简单 Limited Animation
- 灯光
- 雾、雨等环境效果
- EEVEE 预览和正式渲染

第一版默认优先使用 **EEVEE**。

### Blender MCP

用于交互探索：

- 查看当前场景
- 移动物体
- 调整 Camera
- 尝试材质 / 灯光
- 设置简单关键帧
- 获取 viewport / preview
- 快速比较不同方案

定位：

> MCP 是“手”和“草稿台”，适合试。

### Blender Python

用于保存已经确认的重要结果：

- 稳定场景配置
- Camera
- 材质
- 动画
- 渲染设置
- 可重复生成的内容

定位：

> Python 是“记忆”，适合固定和重现。

写 Blender 自动化代码时：

> 优先使用 Blender Data API；非必要不大量依赖 `bpy.ops`，避免 UI context 带来的不稳定。

### Remotion / FFmpeg

负责：

- 最终时间轴
- 无声唱词
- 落款
- 图像序列与音频合成
- 最终 MP4 编码

### Logic / MIDI

负责正式音乐和最终 WAV。

## 5. 实际镜头制作循环

每个镜头优先采用以下最小循环：

### A. 明确镜头目的

先回答：

> 这个镜头要让观众意识到什么 / 感受到什么？

不要先追求“漂亮”。

### B. 粗版本

Codex 在 Blender 中先使用：

- 简单平面
- 方块
- 占位图
- 简单颜色
- 粗 Camera
- 最少动画

先验证：

1. 画面怎么摆
2. 镜头怎么动
3. 时长是否合适
4. 观众注意力在哪里
5. 镜头是否完成叙事任务

Preview 可以丑，但必须尽早产生。

> Preview 可以丑，但不能晚。

### C. 用户直觉反馈

用户只需要说观看感受。

Codex判断问题类型，并优先处理当前最明显的一个问题。

每轮不要同时改很多变量。

### D. 精修

粗镜头成立后再逐步加入：

- 正式素材
- 宣纸 / 剪纸材质
- 灯光
- 景深
- 雨 / 雾
- 细微动画
- 最终颜色

不要用特效修补基础构图或节奏问题。

### E. 固化

确认后的重要设置写回 Blender Python 或稳定的 `.blend` 文件。

## 6. Limited Animation 默认规则

- 每镜头只保留约 1～3 个主要运动焦点。
- Camera 运动优先于复杂肢体动画。
- 不让所有图层同速移动。
- 避免所有镜头都推近。
- 避免匀速、突然开始、突然结束造成 PPT 感。
- 越重要的镜头通常越克制。
- 静止或近乎静止是允许的。
- 先解决构图、空间和 Timing，再增加动画。

## 7. 渲染原则

开发阶段：

- 低分辨率
- 低采样
- 单镜头
- 局部 preview
- 必要时首帧 / 中间帧 / 尾帧

不要频繁完整高质量渲染。

正式阶段：

> Blender 优先输出 PNG / EXR 图像序列，而不是直接输出最终 MP4。

然后：

```text
Blender → PNG / EXR sequence
Logic   → WAV
Remotion / FFmpeg → 文字 + 图像 + 音频 → Final MP4
```

这样渲染中断后可以继续，也方便后期修改。

## 8. 资产与复用

第一版不要提前搭大型资产系统。

当某个内容开始反复出现时，再选择最简单的复用方式：

- 材质 / Object / Collection → Blender Asset Browser
- 重复程序操作 → Python 函数
- 参数化重复几何 → Geometry Nodes
- 当前作品专用内容 → 保留在《梁祝》目录

系列级能力和单片专用内容要逐渐分开，但不提前设计复杂架构。

## 9. 《梁祝》第一轮 Blender 实验

优先制作：

> **“雨中红轿经过孤坟”**

建议先做约 8～12 秒的完整镜头闭环。

这个镜头用于验证：

- 纸影风格
- 2.5D 分层
- Camera
- 视差
- 前景遮挡
- 雨雾
- 红色控制
- 景深
- Limited Animation
- Codex → Blender → Preview → 用户反馈 → 修改 的完整流程

暂时不要先挑战：

- 复杂人物表演
- 相认高潮
- 投坟
- 化蝶
- 复杂布料 / 骨骼 / 物理模拟

如果这个镜头能够做到“不像 PPT，而像一个真正有情绪的电影镜头”，再扩展到后续镜头。

## 10. Codex 的默认工作态度

Codex 应：

- 优先给简单且足够有效的方案。
- 先制作可看的结果，再讨论抽象系统。
- 不因为技术上能实现就增加复杂度。
- 不未经确认改变已经 Locked 的故事和核心阐释。
- 不以复杂度证明质量。
- 发现重复劳动后，再建议抽象。
- 每次修改说明“观看上会发生什么变化”，而不只给技术参数。
- 遇到用户不确定时，可提供：
  - A. 最简单且足够有效
  - B. 效果更好但稍复杂

最终目标：

> 用户负责说“看起来对不对”；Codex 负责逐渐建立“这种感觉为什么产生，以及如何稳定地把它做出来”的制作方法。
