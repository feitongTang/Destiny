# AI Cutscene Project Rules

> 本文件是本项目的最高级制作规则。  
> Codex 在新增功能、修改动画、生成素材清单、调整镜头或重构代码时，应优先遵守本文件。

---

# 1. 项目目标

本项目用于制作约 **2～4 分钟，默认约 3 分钟** 的叙事型短视频。

核心内容是：

- 描绘真实人物或虚构人物的人生片段
- 强调命运转折、冲突、失败、选择、崛起、失去、反转等戏剧性变化
- 视觉形式接近：
  - 二次元游戏剧情过场
  - 动态插画
  - 2D Cutout Animation
  - 2.5D Parallax
  - Limited Animation
  - Motion Comic
  - Multiplane Animation

目标不是制作传统逐帧动画。

目标是：

> 使用少量高质量静态素材，通过图层、镜头、视差、局部运动、光影、粒子和剪辑，制作具有电影感的叙事动画。

---

# 2. 核心制作原则

始终优先采用：

```text
静态插画
↓
拆分图层
↓
程序动画
↓
镜头运动
↓
局部动态
↓
音频与剪辑
↓
最终视频
```

不要默认采用：

```text
提示词
↓
AI 视频模型
↓
整段视频
```

AI 视频生成属于高成本补充工具，不是主工作流。

除非某个镜头确实无法通过静态素材、图层动画或简单变形实现，否则不要建议使用付费 AI 视频服务。

---

# 3. 默认技术栈

第一版项目优先使用：

## 核心

- TypeScript
- React
- Remotion

Remotion 负责：

- 时间轴
- 图层
- 摄像机运动
- 关键帧
- 转场
- 粒子
- 字幕
- 音频同步
- 最终渲染

## 辅助

- FFmpeg
- PNG / WebP / SVG
- WAV / MP3

## 可选开源工具

仅在需要时引入：

- BiRefNet：人物与物体抠图
- SAM / SAM 2：复杂对象分割
- Depth Anything：生成深度信息
- LivePortrait：少量面部动态
- Blender：高级 2.5D 镜头
- Kdenlive：最终人工剪辑

第一版不要因为“以后可能有用”而提前增加复杂依赖。

---

# 4. 用户能力假设

默认认为项目使用者：

- 会描述故事
- 会评价画面好不好看
- 会给出自然语言修改意见
- 不熟悉传统动画软件
- 不需要理解复杂动画数学
- 不应该被要求手动操作大量关键帧
- 不应该被要求学习复杂节点系统

因此：

> 所有复杂动画操作应尽可能封装成简单参数或自然语言可修改的配置。

例如不要要求用户手工写复杂曲线。

优先设计成：

```ts
cameraPush({
  from: 1,
  to: 1.08,
  duration: 6,
  easing: "slow-cinematic",
});
```

而不是暴露大量底层参数。

---

# 5. 3 分钟视频的默认叙事结构

默认目标时长：

```text
150～210 秒
```

推荐：

```text
约 180 秒
```

不要把 3 分钟视频理解成一个连续长镜头。

默认拆成：

```text
12～20 个镜头
```

平均镜头长度约：

```text
5～12 秒
```

少数关键镜头可以达到：

```text
15～20 秒
```

---

# 6. 人物故事的默认戏剧结构

如果用户没有明确指定结构，优先采用：

## 第一幕：钩子

约 0～20 秒。

目的：

> 第一时间告诉观众：这个人物后来发生了一件非同寻常的事情。

可以先展示结果，再回到过去。

例如：

```text
他曾经站在所有人仰望的位置。

三年后，却连自己的名字都不愿提起。
```

画面优先：

- 强烈人物特写
- 关键事件
- 极端处境
- 结果镜头
- 象征性物件

---

## 第二幕：建立人物

约 20～60 秒。

解释：

- 他是谁
- 他想要什么
- 他原本处于什么位置
- 世界如何看待他
- 他最大的优势或弱点是什么

这一部分不要大量堆设定。

只保留后续剧情需要的信息。

---

## 第三幕：上升 / 冲突

约 60～110 秒。

展示：

- 成功
- 野心
- 矛盾
- 关系变化
- 决定
- 外部压力

节奏应逐渐加快。

镜头切换可以增加。

---

## 第四幕：重大转折

约 110～145 秒。

这是整条视频最重要的部分。

通常对应：

- 背叛
- 战败
- 事故
- 失去
- 发现真相
- 做出错误选择
- 巨大成功
- 身份反转
- 命运改变

这一段允许使用最强的视觉效果。

例如：

- 强烈推镜
- 突然静止
- 黑屏
- 高反差
- 水墨扩散
- 闪白
- 快速蒙太奇
- 音乐骤停

---

## 第五幕：余波与结尾

约 145～180 秒。

解释：

> 这件事最终改变了什么？

不要简单复述结果。

结尾最好留下：

- 人物选择
- 历史意义
- 情绪余韵
- 一个讽刺
- 一个问题
- 一个视觉意象

最后一个镜头通常应比前面的镜头更安静。

---

# 7. 真实人物内容规则

如果内容涉及真实人物：

必须区分：

```text
已知事实
合理推断
艺术化重现
虚构内容
```

不得为了戏剧效果，把未经证实的严重指控写成事实。

如果某段：

- 内心活动没有可靠记录
- 对话没有可靠来源
- 事件细节经过艺术加工

应该在脚本或项目元数据中标注：

```text
dramatized: true
```

或采用明确的叙事方式：

```text
也许在那一刻……
```

而不是：

```text
他当时想的是……
```

如果项目属于历史人物或真实事件叙事，应保留：

```text
sources.md
```

记录主要事实来源。

动画可以艺术化。

事实不能随意艺术化。

---

# 8. 虚构人物内容规则

虚构人物可以自由进行戏剧化设计。

但必须保证：

```text
人物目标
↓
人物行动
↓
产生结果
↓
结果改变人物
```

不要仅仅罗列事件。

如果故事只是：

```text
发生 A
发生 B
发生 C
发生 D
```

Codex 应主动指出：

> 当前故事缺乏明确的因果链或人物转变。

---

# 9. 视觉核心：少动，而不是全动

动画设计首先遵循：

> Limited Animation。

一个镜头中通常只允许 **1～3 个主要运动焦点**。

例如：

```text
人物：基本静止
头发：轻微摆动
背景：缓慢视差
镜头：缓慢推进
```

而不是让：

```text
人物
衣服
头发
云
花
灯
建筑
摄像机
粒子
```

全部同时大幅运动。

画面越重要，运动通常越克制。

---

# 10. 默认运动幅度

人物呼吸：

```text
scale: 1.000 → 1.005～1.015
```

人物身体摆动：

```text
rotation: ±0.2° ～ ±1°
```

头发 / 饰品：

```text
rotation: ±1° ～ ±4°
```

悬挂物：

```text
rotation: ±1° ～ ±5°
```

镜头推进：

```text
scale: 1.00 → 1.03～1.12
```

普通横移：

```text
画面宽度的 1%～8%
```

除非属于：

- 战斗
- 梦境
- 崩塌
- 强烈心理变化

否则不要使用剧烈镜头运动。

---

# 11. 2.5D 图层规则

默认场景至少考虑：

```text
background
midground
character
foreground
effects
```

简单镜头可以只使用：

```text
background
character
foreground
```

不同深度的运动速度必须不同。

示例：

```text
远景      0.15
中景      0.35
人物      0.55
前景      1.00
```

目标不是物理准确。

目标是产生空间感。

---

# 12. 图层设计

推荐资产结构：

```text
assets/
└── scene_001/
    ├── background/
    │   ├── sky.png
    │   ├── mountains.png
    │   └── architecture.png
    │
    ├── character/
    │   ├── body.png
    │   ├── head.png
    │   ├── hair_front.png
    │   └── hair_back.png
    │
    ├── foreground/
    │   ├── branches.png
    │   └── flowers.png
    │
    └── effects/
        ├── fog.png
        └── particles.png
```

不要为了理论上的灵活性把一张图拆成几十层。

只有在视觉上需要独立运动的东西才拆分。

---

# 13. 镜头优先于人物动画

如果一个静态人物看起来“不够有动画感”，优先尝试：

1. 摄像机推进
2. 摄像机横移
3. 2.5D 视差
4. 前景遮挡
5. 光线变化
6. 雾气
7. 粒子
8. 呼吸
9. 头发轻摆

最后才考虑复杂人物动作。

---

# 14. 默认镜头语言

项目应提供可复用的镜头预设。

至少包括：

```text
static
slowPushIn
slowPullOut
panLeft
panRight
tiltUp
tiltDown
parallaxPush
characterReveal
foregroundReveal
dramaticPush
handheldSubtle
```

用户应该可以说：

```text
这个镜头缓慢靠近人物。
```

Codex 将其转换成：

```text
slowPushIn
```

用户不需要直接处理摄像机参数。

---

# 15. Easing 规则

默认不要使用匀速运动。

优先：

```text
ease-in-out
slow cinematic easing
smoothstep
cubic easing
```

运动应该有：

```text
开始
↓
逐渐加速
↓
运动
↓
逐渐减速
↓
停止
```

不要产生明显 PPT 感。

---

# 16. 动态插画常用效果

项目应逐步实现以下可复用组件：

## 镜头

```text
CameraPush
CameraPan
CameraShake
ParallaxScene
FocusShift
```

## 人物

```text
Breathing
HairSway
ClothSway
Blink
HeadTilt
```

## 环境

```text
Fog
Dust
Petals
Rain
Snow
Smoke
FloatingParticles
LightRays
```

## 转场

```text
Fade
CrossDissolve
InkTransition
PaperTear
ForegroundWipe
Flash
BlurTransition
```

## 画面质感

```text
FilmGrain
PaperTexture
Vignette
SoftGlow
ChromaticOffset
SubtleBlur
```

所有组件都应尽量：

- 参数少
- 默认值好看
- 可组合
- 可自然语言修改

---

# 17. 不要做 PPT 动画

出现以下特征说明镜头失败：

- 整张图片简单左右移动
- 所有图层速度一致
- 每个镜头都只是在 zoom in
- 每个物体都匀速运动
- 元素突然开始或突然停止
- 每个镜头的运动方式完全相同
- 所有场景都有大量粒子
- 人物像贴纸一样平移

发现这些问题时，应优先修复动画设计，而不是增加更多视觉效果。

---

# 18. 静态镜头是允许的

不要害怕静止。

如果：

- 配音非常重要
- 情绪达到高潮
- 人物刚遭遇重大事件
- 需要让观众消化信息

可以让画面接近静止。

例如：

```text
角色完全静止
+
非常缓慢的镜头推进
+
背景只有轻微雾气
```

有时比复杂动画更有效。

---

# 19. 素材生成原则

优先生成：

> 动画友好的素材。

而不是单纯生成：

> 最漂亮的完整插画。

生成素材时需要考虑：

- 人物不要贴画面边缘
- 被遮挡的身体部分最好完整
- 背景尽量包含更多安全区域
- 前景遮挡物单独生成
- 人物与背景尽量容易分离
- 镜头移动方向需要预留画面空间

例如计划向右 pan：

```text
原始素材右侧必须有额外内容。
```

---

# 20. 画面尺寸

默认最终输出：

```text
1920 × 1080
```

但生成静态素材时尽量使用更高分辨率。

推荐：

```text
2560 × 1440
```

或更高。

这样才能支持：

- 推镜
- 横移
- 裁切
- 轻微旋转

而不会快速失去清晰度。

---

# 21. 帧率

默认：

```text
24 fps
```

不要因为画面是二次元就默认使用 60fps。

部分元素可以模拟：

```text
12fps
8fps
```

的有限动画。

例如头发或纸片动作可以每两帧更新一次。

摄像机仍保持平滑。

这样可以产生：

> 插画动画 / 手绘动画的节奏感。

---

# 22. 旁白是时间轴骨架

对于人物故事：

> 音频优先于动画。

推荐流程：

```text
脚本
↓
旁白
↓
确认旁白时长
↓
建立时间轴
↓
设计镜头
↓
制作动画
```

不要先做 3 分钟动画，再强行塞旁白。

---

# 23. 镜头表

每个项目都应该维护：

```text
storyboard.md
```

至少包含：

| Shot | 时间 | 旁白 | 画面 | 镜头 | 动画重点 |
|---|---:|---|---|---|---|
| 01 | 0-8s | … | … | Push In | 雨 |
| 02 | 8-15s | … | … | Pan | 头发 |
| 03 | 15-24s | … | … | Static | 烟 |

不要直接进入代码。

先明确镜头。

---

# 24. Scene 配置

每个镜头尽量使用数据描述。

例如：

```ts
{
  id: "scene_03",
  duration: 8,

  camera: {
    preset: "slowPushIn",
    strength: 0.4
  },

  layers: [
    {
      src: "background.png",
      depth: 0.2
    },
    {
      src: "character.png",
      depth: 0.6
    },
    {
      src: "foreground.png",
      depth: 1
    }
  ],

  effects: [
    {
      type: "fog",
      intensity: 0.25
    }
  ]
}
```

优先修改配置。

只有新功能无法通过配置实现时才修改核心代码。

---

# 25. 项目目录

默认：

```text
project/
│
├── AGENTS.md
├── README.md
├── storyboard.md
├── script.md
├── sources.md
│
├── public/
│   └── assets/
│
├── src/
│   ├── compositions/
│   │
│   ├── scenes/
│   │
│   ├── components/
│   │
│   ├── effects/
│   │
│   ├── transitions/
│   │
│   ├── presets/
│   │
│   └── utils/
│
├── audio/
│
├── renders/
│
└── package.json
```

---

# 26. 代码设计原则

动画组件必须：

- 小
- 独立
- 可复用
- 可配置
- 有合理默认值

避免：

```text
一个 1500 行的 Scene.tsx
```

推荐：

```text
Scene
├── Camera
├── Background
├── Character
├── Foreground
├── Atmosphere
└── ColorGrade
```

---

# 27. 优先实现预设，而不是参数面板

如果出现：

```text
cameraSpeed
cameraAcceleration
cameraDamping
cameraFriction
cameraEaseA
cameraEaseB
```

说明 API 过于复杂。

用户更适合：

```text
slow
medium
dramatic
subtle
```

Codex 在内部转换成具体参数。

---

# 28. 修改镜头时的工作方式

当用户说：

> 人物出现得太突然。

不要只理解成：

```text
opacity 改慢一点
```

应分析：

- 是否需要前景遮挡 reveal
- 是否需要镜头先看到环境
- 是否需要角色从阴影出现
- 是否需要推镜
- 是否需要调整剪辑点

动画问题首先作为：

> 镜头语言问题

处理。

其次才是代码问题。

---

# 29. 美术一致性

同一视频中尽量维持：

- 相似线条
- 相似人物比例
- 相似光照
- 相似色彩逻辑
- 相似材质
- 相似背景复杂度

如果 AI 生成的两个镜头角色差异明显：

不要直接进入动画。

先修复素材一致性。

---

# 30. AI 视频使用规则

AI 视频只考虑用于：

- 极复杂布料
- 水
- 火
- 大规模环境变化
- 人群
- 无法通过图层实现的人体动作
- 梦境
- 超现实片段
- 关键高潮镜头

默认整条 3 分钟视频中：

```text
AI 视频镜头数量 = 0
```

如果使用：

推荐只占：

```text
总时长 < 10%
```

---

# 31. 镜头价值原则

每一个镜头都应该至少完成一件事：

```text
提供信息
塑造人物
推动剧情
制造情绪
强调转折
建立世界
创造视觉记忆点
```

如果一个镜头什么都没有完成：

删除。

---

# 32. 转场原则

默认优先使用：

```text
直接剪辑
淡入淡出
遮挡转场
构图匹配
运动匹配
```

特殊转场：

```text
水墨
纸张
闪白
粒子
故障
```

只在剧情需要时使用。

不要为了“高级感”大量堆转场。

---

# 33. 音乐与音效

画面的冲击力很大一部分来自声音。

至少考虑：

```text
旁白
背景音乐
环境音
关键音效
```

例如：

```text
雨声
风
脚步
金属碰撞
纸张
火焰
城市环境
鸟鸣
心跳
```

重大剧情变化时，可以优先修改：

> 声音。

不一定修改动画。

---

# 34. 自动化优先

凡是重复操作，都优先交给程序。

例如：

- 批量改变图片尺寸
- 自动检查透明通道
- 自动检查素材缺失
- 自动生成缩略图
- 批量渲染镜头
- 合并镜头
- 音量标准化
- 输出 MP4
- 导出预览版

不要要求用户重复手工执行。

---

# 35. Preview 优先

任何动画功能都必须能快速预览。

制作顺序：

```text
低质量预览
↓
确认镜头
↓
确认节奏
↓
确认素材
↓
最终高质量渲染
```

不要频繁完整渲染 3 分钟视频。

开发阶段优先：

```text
单镜头预览
低分辨率
局部时间范围
```

---

# 36. 每次制作新视频的标准流程

Codex 应引导项目遵循：

```text
STEP 1
确定人物与主题

STEP 2
编写约 3 分钟旁白脚本

STEP 3
确定人物故事的核心转折

STEP 4
拆成 12～20 个镜头

STEP 5
生成 storyboard.md

STEP 6
列出所需静态素材

STEP 7
生成 / 收集素材

STEP 8
自动抠图与整理图层

STEP 9
建立 Remotion 场景

STEP 10
制作基础 Camera + Parallax

STEP 11
加入局部动态

STEP 12
加入环境效果

STEP 13
加入配音

STEP 14
加入音乐与音效

STEP 15
检查节奏

STEP 16
最终渲染
```

---

# 37. 第一版项目的明确边界

第一版优先实现：

- 图片图层
- 视差
- 摄像机
- 基础 easing
- 呼吸
- 摆动
- 雾
- 雨
- 雪
- 粒子
- 花瓣
- 简单光效
- 字幕
- 基础转场
- 音频
- 预览
- 渲染

暂时不要优先实现：

- 完整骨骼动画系统
- IK
- 复杂人物动作
- 3D 角色
- 实时物理
- AI 视频生成平台整合
- 大型节点编辑器
- 自制动画 GUI
- 专业调色系统

第一版的目标是：

> 尽快完成第一条完整的 3 分钟视频。

而不是：

> 先开发一个完整动画软件。

---

# 38. Codex 的角色

Codex 在这个项目中同时承担：

```text
技术导演
动画工程师
工具开发者
自动化工程师
```

但不是故事最终决策者。

当多个方案都可行时：

优先提供：

```text
A. 最简单
B. 效果更好但稍复杂
```

不要未经必要就选择最复杂的方案。

---

# 39. 用户自然语言优先

用户可能会说：

```text
这里再压抑一点。

这个镜头太 PPT 了。

人物不要动这么多。

这里应该有种命运突然改变的感觉。

这个镜头再停一会。

让背景显得更深。

结尾再空一点。
```

Codex 应主动把这些描述翻译成：

- Camera
- Timing
- Easing
- Layer
- Lighting
- Composition
- Effects
- Audio

修改。

不要要求用户自己给出技术参数。

---

# 40. 最重要的判断标准

最终判断一个镜头是否成功，不是：

```text
使用了多少技术
```

而是：

```text
这个镜头是否帮助观众感受到人物此刻发生了什么。
```

如果：

> 一张静止插画 + 一句旁白 + 一次极慢的推镜

比：

> 十种粒子 + 三层动画 + AI 视频

更有效，

选择前者。

---

# 41. 项目最高原则

始终遵循：

> Story First.

然后是：

> Composition.

然后是：

> Camera.

然后是：

> Limited Motion.

最后才是：

> Effects.

即：

```text
故事
>
构图
>
镜头
>
局部动画
>
特效
```

本项目追求的不是动画复杂度。

而是：

> **用最低的动画成本，把人物命运变化讲得有力量。**